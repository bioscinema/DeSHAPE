% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dade_perm_pair.R
\name{dade_perm_pair}
\alias{dade_perm_pair}
\title{Permutation-Based Group Comparison (Binary)}
\usage{
dade_perm_pair(
  formula,
  data,
  mode = c("center", "dispersion", "skewness"),
  alternative = c("greater", "less", "two.sided"),
  perm = 999,
  seed = NULL
)
}
\arguments{
\item{formula}{A formula of the form `response ~ group`, where the group variable has exactly two levels.}

\item{data}{A data frame containing the variables in the formula.}

\item{mode}{Type of test to perform. Must be one of `"center"`, `"dispersion"`, or `"skewness"`.}

\item{alternative}{A character string specifying the alternative hypothesis.
One of `"greater"`, `"less"`, or `"two.sided"`. Only used for the center test.}

\item{perm}{Number of permutations. Default is 999.}

\item{seed}{Random seed for reproducibility. Default is NULL.}
}
\value{
This function prints the permutation p-value to the console.
}
\description{
Performs a permutation-based statistical test to compare two groups on center (median),
dispersion (IQR-based Brown-Forsythe variant), or asymmetry (quantile skewness).
}
\examples{
\dontrun{
data <- data.frame(Shannon = rnorm(100), Group = rep(c("A", "B"), 50))
dade_perm_pair(Shannon ~ Group, data = data, mode = "center")
}
}
